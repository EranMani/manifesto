# Claude-Direct Execution Brief - C46

**Owner:** Rex
**Graph:** `cache`
**Initial context:** 5 files, ~12000 characters

## Goal
Preload a bundled mock policy corpus into query-ready policy documents and chunks.

## Planned Files
- `backend/app/data/demo_policies.json` - commit spec change table; read: full file
- `backend/seed.py` - commit spec change table; commit spec initial_context; read: targeted excerpt; source exceeds 3000 characters
- `backend/tests/test_seed.py` - commit spec change table; commit spec initial_context; read: targeted excerpt; source exceeds 3000 characters

## Preselected Supporting Context
- `backend/app/models/policy.py` [contract] - commit spec initial_context; read: targeted excerpt; source exceeds 3000 characters
- `backend/app/services/ingestion.py` [contract] - commit spec initial_context; read: targeted excerpt; source exceeds 3000 characters

## Contract
Bundle procurement approvals, shipment delays, damaged goods, partial delivery, returns,
lost shipments, and employee escalation policies. Seed deterministic ready documents and
chunks with source title, section, chunk index, active embedding profile, and embeddings
obtained through the configured `EmbeddingService`; skip unchanged ready documents.

## Verification
docker compose run --rm backend uv run pytest tests/test_seed.py -k bundled_policy -q

## Boundaries
- Do not edit `frontend/`
- Do not edit `backend/app/api/`

## Context Rules
- Start with this brief and the selected files. Do not scan directories.
- Use targeted excerpts exactly where specified.
- Expand only for an unresolved symbol, missing contract, failing test, or contradictory evidence.
- Before expansion, record the reason, exact query or path, and expected decision.
- Stop reading when the contract and verification path are sufficiently supported.
