# Claude-Direct Execution Brief - C66

**Owner:** Rex
**Graph:** `cache`
**Initial context:** 6 files, ~12861 characters

## Goal
Enrich graph nodes with optional status and status-category metadata so the frontend can color-code nodes by lifecycle state.

## Planned Files
- `backend/app/api/v1/assistant.py` - commit spec change table; commit spec initial_context; read: targeted excerpt; source exceeds 3000 characters
- `backend/app/schemas/assistant.py` - commit spec change table; commit spec initial_context; read: full file
- `backend/app/services/rag_logistics.py` - commit spec change table; commit spec initial_context; read: targeted excerpt; source exceeds 3000 characters
- `backend/tests/api/test_assistant.py` - commit spec change table; read: targeted excerpt; source exceeds 3000 characters

## Preselected Supporting Context
- `backend/app/models/shipment.py` [contract] - commit spec initial_context; read: full file
- `backend/app/services/__init__.py` [structural] - structural anchor for backend/app/services/; read: full file

## Contract
- `GraphNode` dataclass gains `status: str | None = None` and `status_category: str | None = None`.
- `GraphNodeSchema` Pydantic model gains `status: str | None = None` and `status_category: str | None = None`.
- `_status_category(status: str) -> str` maps:
- `"delivered"` -> `"done"`
- `"pending"`, `"in_transit"` -> `"active"`
- `"delayed"`, `"damaged"`, `"partial"`, `"cancelled"`, `"returned"`, `"lost"` -> `"issue"`
- Shipment nodes: `status=evidence.shipment.status`, `status_category=_status_category(status)`.
- Event nodes: `status=event.event_type`, `status_category="issue"` for exception event types, `"active"` otherwise.
- Buyer, vendor, purchase_order, product nodes: `status=None`, `status_category=None`.
- The API response is backward-compatible (new nullable fields default to `None`).

## Verification
docker compose run --rm backend uv run pytest tests/api/test_assistant.py -q

## Boundaries
- Do not edit `frontend/`
- Do not edit `backend/app/models/`

## Context Rules
- Start with this brief and the selected files. Do not scan directories.
- Use targeted excerpts exactly where specified.
- Expand only for an unresolved symbol, missing contract, failing test, or contradictory evidence.
- Before expansion, record the reason, exact query or path, and expected decision.
- Stop reading when the contract and verification path are sufficiently supported.
