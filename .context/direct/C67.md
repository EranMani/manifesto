# Claude-Direct Execution Brief - C67

**Owner:** Aria
**Graph:** `cache`
**Initial context:** 5 files, ~9543 characters

## Goal
Redesign the evidence graph for immediate readability: wider layout, logistics-aware node colors with status-based green/orange/red encoding, multi-column non-linear layout, and larger graph area.

## Planned Files
- `frontend/src/api/assistant.ts` - commit spec change table; commit spec initial_context; read: full file
- `frontend/src/components/EvidenceGraph.tsx` - commit spec change table; commit spec initial_context; read: targeted excerpt; source exceeds 3000 characters
- `frontend/src/pages/Assistant.tsx` - commit spec change table; commit spec initial_context; read: targeted excerpt; source exceeds 3000 characters

## Preselected Supporting Context
- `frontend/src/App.tsx` [structural] - structural anchor for frontend/src/pages/; structural anchor for frontend/src/components/; read: full file
- `frontend/src/api/client.ts` [structural] - structural anchor for frontend/src/api/; read: full file

## Contract
### Layout changes (`Assistant.tsx`)
- Evidence panel: increase from `lg:w-96` (384px) to `lg:w-[540px]` (540px).
- Graph container: increase from `h-96` (384px) to `h-[500px]` (500px).
### Color scheme (`EvidenceGraph.tsx`)
Replace `TYPE_COLORS` and `TYPE_ORDER` with logistics-aware mappings:
**Node type colors** (base colors when no status):
- `buyer`: blue (`bg: #dbeafe`, `border: #3b82f6`)
- `purchase_order`: amber (`bg: #fef3c7`, `border: #f59e0b`)
- `vendor`: purple (`bg: #ede9fe`, `border: #8b5cf6`)
- `shipment`: slate (`bg: #f1f5f9`, `border: #64748b`) — overridden by status color

## Verification
npm run --prefix frontend build

## Boundaries
- Do not edit `backend/`

## Context Rules
- Start with this brief and the selected files. Do not scan directories.
- Use targeted excerpts exactly where specified.
- Expand only for an unresolved symbol, missing contract, failing test, or contradictory evidence.
- Before expansion, record the reason, exact query or path, and expected decision.
- Stop reading when the contract and verification path are sufficiently supported.
