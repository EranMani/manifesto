# Claude-Direct Execution Brief - C66A

**Owner:** Claude
**Graph:** `cache`
**Initial context:** 2 files, ~6000 characters

## Goal
Auto-generated `.context/direct/CNN.md` briefs are excluded from `actual_scope`'s
changed-file count and diff-line accounting, matching the existing exclusion for
`.context/finalize/*.json` and `.context/direct/*-override.json`.

## Planned Files
- `hooks/tests/test_verify_constraints.py` - commit spec change table; commit spec initial_context; read: targeted excerpt; source exceeds 3000 characters
- `hooks/verify_constraints.py` - commit spec change table; commit spec initial_context; read: targeted excerpt; source exceeds 3000 characters

## Preselected Supporting Context
- None.

## Contract
In `_is_excluded_artifact(path)`, add a check:
if path.startswith(".context/direct/C") and path.endswith(".md"):
return True
This covers `C66.md`, `C66A.md`, `C100.md`, etc. The existing
`.context/direct/*-override.json` exclusion remains unchanged.

## Verification
python -m pytest hooks/tests/test_verify_constraints.py -q

## Boundaries
- Do not edit `frontend/`
- Do not edit `backend/`

## Context Rules
- Start with this brief and the selected files. Do not scan directories.
- Use targeted excerpts exactly where specified.
- Expand only for an unresolved symbol, missing contract, failing test, or contradictory evidence.
- Before expansion, record the reason, exact query or path, and expected decision.
- Stop reading when the contract and verification path are sufficiently supported.
