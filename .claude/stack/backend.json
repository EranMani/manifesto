{
  "domain": "backend",
  "owner": "rex",
  "language": "python",
  "version": "3.12+",
  "package_manager": "uv",
  "framework": "fastapi",
  "validation": "pydantic",
  "orm": "sqlalchemy",
  "migrations": "alembic",
  "async_workers": "celery",
  "message_broker": "redis",
  "task_scheduling": "celery-beat",
  "testing": "pytest",
  "patterns": [
    "service-layer: business logic lives in services, not routes",
    "repository-pattern: database access abstracted behind repository interfaces",
    "dependency-injection: FastAPI Depends() for composable dependencies",
    "separation-of-concerns: routes handle HTTP, services handle logic, models handle data",
    "error-hierarchy: custom exception classes with typed error responses",
    "async-by-default: async endpoints, sync only when forced by a blocking library"
  ],
  "principles": [
    "Routes are thin — validate input, call service, return response.",
    "Never import provider SDKs outside the service layer.",
    "Background tasks go through Celery, not FastAPI BackgroundTasks (except fire-and-forget logging).",
    "Every endpoint has input validation (Pydantic), output schema, and error responses documented.",
    "Migrations are always reversible — every upgrade has a matching downgrade.",
    "Environment variables for all configuration — never hardcode secrets, URLs, or feature flags."
  ],
  "validation_framework": {
    "library": "pydantic",
    "role": "runtime enforcement — not advisory type hints, but active validation that raises errors immediately on invalid data",
    "principle": "Treat every external input as a potential failure point. Shift validation to the absolute source of data entry.",
    "core_patterns": {
      "basemodel_over_dataclass": "Replace all @dataclass with BaseModel for external data — dataclass type hints are advisory (silent failures), BaseModel enforces at runtime",
      "strict_mode": "Use ConfigDict(strict=True) for high-integrity systems where type coercion ambiguity is a risk",
      "field_governance": "Use Field() for business rule enforcement — gt/le for numeric bounds, min_length/max_length for strings, pattern for regex validation",
      "literal_types": "Use Literal['draft', 'pending', 'shipped'] for categorical fields — eliminates magic string bugs across the application",
      "nested_validation": "Pydantic validates the entire tree — parent models containing child models cascade validation down through all levels",
      "reverse_validation": "Use model_validate() or ** unpacking to safely ingest raw data from databases and external APIs"
    },
    "lifecycle": [
      "1. instantiation: User(id=1, ...) triggers validation engine immediately",
      "2. validation: raises ValidationError if types or constraints fail",
      "3. transformation: model_dump() exports to standard Python dict",
      "4. serialization: model_dump_json() converts to API-ready JSON string"
    ],
    "settings_management": {
      "library": "pydantic-settings",
      "pattern": "Inherit from BaseSettings to auto-load environment variables at boot time",
      "secret_handling": "Use SecretStr for sensitive data — values masked with asterisks in logs",
      "boot_time_validation": "Instantiate settings at application boot — missing config fails immediately, preventing partial system starts",
      "env_file": "ConfigDict(env_file='.env', env_file_encoding='utf-8')"
    },
    "ai_structured_outputs": {
      "purpose": "Pydantic is the hard stop for LLM hallucinations — forces AI responses into deterministic structure",
      "patterns": [
        "response-format: pass Pydantic model into LLM response_format to force validated JSON output",
        "field-descriptions-as-prompts: Field(description='...') serves as prompt metadata — guides LLM understanding during JSON generation, making code the single source of truth",
        "self-correction-loop: feed ValidationError messages back to LLM programmatically — agent retries until producing valid schema"
      ],
      "principle": "Code is the source of truth for both Python logic and the AI's worldview — Field descriptions guide both validation and LLM generation."
    },
    "day_one_checklist": [
      "map every external input (API, DB, webhook) to a BaseModel",
      "replace all @dataclass structures handling external data with BaseModel",
      "implement BaseSettings for all infrastructure credentials with boot-time validation",
      "apply Literal types for all categorical variables — eliminate magic strings",
      "add Field constraints (gt, le, min_length, pattern) for all business-rule-bound fields"
    ]
  },

  "database": {
    "primary": "postgresql",
    "version": "15+",
    "vector_extension": "pgvector",
    "cache": "redis",
    "patterns": [
      "single-database: PostgreSQL handles relational data AND vectors (pgvector) — no separate vector DB needed to start",
      "connection-pooling: use connection pool for all database access",
      "migration-driven: all schema changes go through Alembic, never manual SQL",
      "soft-delete: prefer soft deletes (is_deleted flag) over hard deletes for audit trail",
      "indexed-queries: every query that filters or joins should have a supporting index"
    ],
    "principles": [
      "PostgreSQL is enough — Instagram scaled to hundreds of millions on just Postgres.",
      "Don't add a separate vector database until pgvector becomes a proven bottleneck.",
      "Redis is for caching and message brokering, not as a primary data store.",
      "Every table has created_at, updated_at timestamps.",
      "Foreign keys are enforced at the database level, not just the application level.",
      "Use UUIDs for public-facing IDs, auto-increment integers for internal references."
    ],
    "scaling_path": [
      "Start: single PostgreSQL instance with pgvector",
      "Growth: add read replicas, connection pooling (PgBouncer)",
      "Scale: partition large tables, consider dedicated vector DB (Weaviate/Chroma) only when pgvector query time exceeds SLA"
    ]
  },

  "human_in_the_loop": {
    "description": "Backend patterns for halting agent execution before side effects and resuming after human approval.",
    "control_mechanisms": {
      "llm_as_router": {
        "pattern": "LLM generates a structured action plan (Pydantic model with list of actions), backend validates and enforces safety rules",
        "enforcement": "Use Pydantic model_validator to enforce business rules — e.g., if transfer.amount > 100, automatically set requires_confirmation = True regardless of LLM reasoning",
        "reliability": "very high — logic is in the backend validation layer, not in the LLM's reasoning",
        "best_for": "structured workflows with predictable decision paths"
      },
      "agentic_tool_calling": {
        "pattern": "LLM selects tools dynamically, backend intercepts tool calls post-generation but pre-execution within a while loop",
        "enforcement": "inspect requested parameters against business logic before calling execute — break the loop if halt required",
        "reliability": "moderate — depends on model's tool-selection accuracy",
        "best_for": "open-ended agents requiring diverse, dynamic toolsets"
      }
    },
    "state_serialization": {
      "description": "Persist full agent context to database when execution halts — enables stateless resume by any worker.",
      "required_fields": [
        "agent_context: full system prompt and internal state at the moment of halt",
        "message_history: complete transcript for conversational coherence",
        "pending_tool_arguments: specific JSON parameters (e.g., recipient_id, amount) generated by LLM, stored for execution without re-running inference",
        "halt_reason: why execution was paused (threshold exceeded, requires approval, ambiguous intent)",
        "created_at: timestamp for timeout and cleanup logic"
      ],
      "resume_pattern": "dedicated execute endpoint loads context by unique ID from database, executes pending action — any available worker can resume"
    },
    "principles": [
      "Never execute a side-effect-heavy function without explicit approval gate for high-stakes actions.",
      "Deferred execution: capture the intent to act, validate it, defer execution until approval condition is met.",
      "State serialization prevents the agent from 'losing its mind' when the execution environment is cycled.",
      "The execute endpoint is separate from the chat endpoint — resume is a distinct API path, not a continuation of the original request."
    ]
  },

  "mcp_server_architecture": {
    "description": "Enterprise-grade MCP server pattern for exposing Python functions as LLM-accessible tools.",
    "layers": {
      "server_layer": {
        "file": "server.py",
        "responsibility": "registration hub — initializes MCP server instance and exposes Python functions as tools to the LLM",
        "principle": "server.py contains ONLY tool registration and metadata. No business logic."
      },
      "service_layer": {
        "file": "source/service.py",
        "responsibility": "Service Class Pattern — wraps external APIs in a dedicated class, decouples business logic from the MCP protocol",
        "benefit": "swap an API provider (e.g., YouTube to Vimeo) by changing only the service layer — protocol and utility layers remain untouched"
      },
      "utility_layer": {
        "file": "utils/helpers.py",
        "responsibility": "supporting logic independent of core service — regex parsers, input sanitizers, formatters"
      }
    },
    "directory_layout": [
      ".env                — environment variables (API keys, proxies)",
      "pyproject.toml      — UV project configuration",
      "server.py           — MCP server registration and tool definitions",
      "test.py             — functional verification script",
      "source/service.py   — core logic (Service Class)",
      "utils/helpers.py    — independent utility functions"
    ],
    "tool_metadata": {
      "principle": "Tool descriptions in server.py must be hyper-specific — the LLM relies on these to decide which tool to call and how to format arguments.",
      "patterns": [
        "describe WHAT the tool does and WHEN to call it — not implementation details",
        "non-overlapping purposes across tools — ambiguity in descriptions causes tool-selection failures",
        "input parameter descriptions guide the LLM's argument formatting"
      ]
    },
    "testing_protocol": {
      "stage_1": "direct functional testing — call service methods from test.py to validate Python logic without the protocol layer",
      "stage_2": "protocol validation — run 'mcp dev server.py' to launch MCP Inspector, verify tools are visible and callable with dry-run arguments"
    },
    "reliability": [
      "input-sanitization: clean and validate inputs in utility functions before they reach the service layer",
      "proxy-integration: implement proxy support via environment variables for external API calls to prevent IP banning",
      "error-messages: return human-readable errors from tools, never raise unhandled exceptions — the LLM interprets and course-corrects"
    ]
  },

  "key_libraries": [
    "fastapi: API framework with automatic OpenAPI docs",
    "pydantic: runtime data validation, schema enforcement, and settings management",
    "sqlalchemy: ORM with async support",
    "alembic: database migration management",
    "celery: distributed task queue for background processing",
    "redis: message broker for Celery + caching layer",
    "uvicorn: ASGI server",
    "httpx: async HTTP client for external API calls",
    "tenacity: retry logic for external service calls",
    "structlog: structured logging",
    "pydantic-settings: environment variable loading with boot-time validation and SecretStr masking",
    "email-validator: EmailStr and domain validation for Pydantic models",
    "mcp[cli]: Model Context Protocol server framework for exposing Python functions as LLM tools"
  ]
}
